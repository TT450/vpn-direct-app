/**
 * Tracked public stub. Do not import *.local.ets here.
 * Private machines: replace this file to register DirectBackendRuntime.localInstaller,
 * then: git update-index --skip-worktree harmony/entry/src/main/ets/backend/DirectBackendLocal.ets
 */
export function ensureLocalBackendRegistered(): void {
  // Public no-op. Private override registers DirectBackendRuntime.localInstaller.
}
