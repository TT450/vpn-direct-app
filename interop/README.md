# Interop

Manual / optional live network checks against real servers.

Fixtures under `tests/fixtures/regression/` are secret-free samples for `sing-box check` and builder unit tests. Mark live runs in CI as optional — Core 0.1 ships fixtures + check scripts without requiring reachable peers.
