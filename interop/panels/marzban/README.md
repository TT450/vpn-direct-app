# Interop lab — Marzban

Repo: https://github.com/Gozargah/Marzban  
Install: Marzban-scripts `marzban.sh`  
Dossier: `docs/compatibility/panels/marzban.md`

## Run

Prefer upstream install script on a disposable VPS; compose stub below is optional for API-only experiments.

```bash
cp .env.example .env
./run.sh
```

Swagger: `${PANEL_URL}/docs`
