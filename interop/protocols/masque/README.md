# Interop lab — masque

- **masque-connect-ip**: Core capability path; lab profile import tooling.
- **warp-masque**: parser stress from public WARP-Config repos; live Cloudflare via env only
  (see `docs/battle/EXTERNAL_CREDENTIALS.md`).

Never commit WARP identity files.
