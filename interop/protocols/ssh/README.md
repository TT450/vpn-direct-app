# Interop lab — ssh

SSH password + key test accounts on a lab host only.

**Never** use random public SOCKS/HTTP/SSH. Local deterministic accounts only.

## Run

```bash
cp .env.example .env && set -a && source .env && set +a
./run.sh
```
